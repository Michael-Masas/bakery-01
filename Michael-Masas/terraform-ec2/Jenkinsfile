pipeline {
agent any
  stages {
    stage('Create Source') {
      steps {
        sh 'cd /tmp'
        sh 'mkdir source'
        sh 'git clone "https://github.com/Michael-Masas/bakery-01.git"'
      }
    }
  }
}
