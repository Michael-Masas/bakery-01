pipeline {
agent any
  stages {
    stage('Create Source') {
      steps {
        sh 'cd ~'
        sh  'mkdir 'source''
      }
    }
    stage('git clone') {
      steps {
        git clone "https://github.com/Michael-Masas/bakery-01.git"
      }
    }
  }
}
